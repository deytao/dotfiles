#!/bin/bash

FILE=$1
PATTERNS=`grep function $FILE | cut -d' ' -f4 | cut -d'(' -f1 | sort`

for PATTERN in $PATTERNS;
do
	echo $PATTERN
	search $PATTERN | cut -d: -f1 | sort | uniq -c | grep -v "1 ./$FILE" | grep -v 'tests/'
done 