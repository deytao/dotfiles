#!/bin/bash

if [ ! -d optimized ]; then
	mkdir optimized
fi

for FILE in `ls -1 *.png`
do
	pngcrush -dir optimized -rem alla -brute -reduce $FILE
done;

echo ""
echo "Before: " "$(du -c -b *.png | grep total -)" 
echo "After:  " "$(du -c -b optimized/*.png | grep total -)"
echo "Crushing done."
